package com.example.nontonkuy.ui.detail

import com.example.nontonkuy.utils.DataDummy
import org.junit.Assert.*
import org.junit.Before
import org.junit.Test

class DetailSeriesViewModelTest {
//    private lateinit var viewModel: DetailSeriesViewModel
//    private val dummySeries = DataDummy.generateDummySeries()[0]
//    private val seriesId = dummySeries.seriesId
//
//    @Before
//    fun setup() {
//        viewModel = DetailSeriesViewModel()
//        viewModel.setSelectedSeries(seriesId)
//    }
//
//    @Test
//    fun getMovie() {
//        viewModel.setSelectedSeries(dummySeries.seriesId)
//        val seriesEntity = viewModel.getSeries()
//        assertNotNull(seriesEntity)
//        assertEquals(dummySeries.seriesId, seriesEntity.seriesId)
//        assertEquals(dummySeries.posterPath, seriesEntity.posterPath)
//        assertEquals(dummySeries.title, seriesEntity.title)
//        assertEquals(dummySeries.genre, seriesEntity.genre)
//        assertEquals(dummySeries.rating.toString(), seriesEntity.rating.toString())
//        assertEquals(dummySeries.startYear, seriesEntity.startYear)
//        assertEquals(dummySeries.finishYear, seriesEntity.finishYear)
//        assertEquals(dummySeries.desc, seriesEntity.desc)
//    }
}